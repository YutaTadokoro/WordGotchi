# Claude API Endpoint (required - proxy server endpoint)
# Use relative path to go through Vite proxy: /api/custom-claude
# Or use absolute URL (requires CORS on server): http://localhost:8000
VITE_CLAUDE_API_ENDPOINT=/api/custom-claude

# Claude API Proxy Target (for Vite proxy configuration)
# Example: http://127.0.0.1:8000
VITE_CLAUDE_PROXY_TARGET=http://127.0.0.1:8000

# Note: Claude API key is managed by the proxy server
# The proxy server should handle authentication

# Gemini API Endpoint (required - proxy server endpoint)
# Use relative path to go through Vite proxy: /api/custom-gemini
# Or use absolute URL (requires CORS on server): http://localhost:8001
VITE_GEMINI_API_ENDPOINT=/api/custom-gemini

# Gemini API Proxy Target (for Vite proxy configuration)
# Example: http://127.0.0.1:8001
VITE_GEMINI_PROXY_TARGET=http://127.0.0.1:8001

# Note: Gemini API key and Google Cloud project ID are managed by the proxy server
# The proxy server should handle authentication and project configuration
